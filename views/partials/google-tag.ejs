<script>
  // Analytics is non-critical: keep it out of the critical rendering path.
  (() => {
    const load = () => {
      if (window.__gtagLoaded) return;
      window.__gtagLoaded = true;
      window.dataLayer = window.dataLayer || [];
      window.gtag = window.gtag || function(){ dataLayer.push(arguments); };
      gtag('js', new Date());
      gtag('config', 'G-2ERQGYPM39');
      const script = document.createElement('script');
      script.async = true;
      script.src = 'https://www.googletagmanager.com/gtag/js?id=G-2ERQGYPM39';
      document.head.appendChild(script);
    };
    const schedule = window.requestIdleCallback || ((callback) => setTimeout(callback, 2500));
    schedule(load);
    ['pointerdown', 'keydown', 'touchstart'].forEach((event) => window.addEventListener(event, load, { once: true, passive: true }));
  })();
</script>
