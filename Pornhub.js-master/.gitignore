.DS_Store
node_modules
# The application imports the built ESM bundle at runtime. Keep it in the
# repository because the parent application does not build this vendored
# package during deployment.
dist
!dist/
!dist/index.js
!dist/index.mjs
tmp_release

# local env files
.env.local
.env.*.local

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor directories and files
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw*

# Local experienment files
src/playground.*
_dump
